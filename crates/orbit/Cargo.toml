[package]
name = "orbit-domain"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
orbit-platform = { path = "../platform" }
serde_json = "1"
pulldown-cmark = { version = "0.13", default-features = false }
